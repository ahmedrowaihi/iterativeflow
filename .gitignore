node_modules
dist
*.tsbuildinfo
coverage

# local Claude Code state (agent worktrees, settings)
.claude/
yarn.lock

# local-only working docs: design notes, field reports, ADR drafts, case studies, the docs plan
docs/
