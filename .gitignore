node_modules
dist
*.tsbuildinfo
coverage

# local Claude Code state (agent worktrees, settings)
.claude/
yarn.lock

# local-only working docs: design notes, field reports, ADR drafts, case studies, the docs plan.
# docs/v2/*.md is NOT local — the published package READMEs link to CONTRACTS/RECOVERY/MIGRATION.
docs/adr/
docs/v2/notes/
docs/DOCS_PLAN.md
