# @iterativeflow/dynamodb — public API

> Generated by `scripts/api-snapshot.mjs`. Do not edit by hand.

## index.d.mts

```ts
import { DynamoDBDocumentClient } from "@aws-sdk/lib-dynamodb";
import { CreateTableCommandInput, DynamoDBClient } from "@aws-sdk/client-dynamodb";
import { Backend, IdGen } from "@iterativeflow/core/backend";
//#region src/client.d.ts
/**
 * The minimal send-surface the backend drives. The concrete {@link DynamoDBDocumentClient}
 * satisfies it, and so does any wrapper (e.g. a fault-injecting proxy in tests) — the backend
 * never depends on the client class, only on `send`.
 */
interface Doc {
  send(command: unknown): Promise<unknown>;
}
/**
 * Wrap a low-level {@link DynamoDBClient} in a document client that (un)marshals plain JS.
 * `removeUndefinedValues` keeps optional attributes (lease token, error) off the item rather
 * than rejecting the write; the backend JSON-encodes user payloads itself, so nested shapes
 * round-trip exactly regardless of Dynamo's native type coercion.
 */
declare const docClient: (low: DynamoDBClient) => DynamoDBDocumentClient;
//#endregion
//#region src/schema.d.ts
/** Default single table. All entities live here so an outbox is one `TransactWriteItems`. */
declare const DEFAULT_TABLE = "iterativeflow";
/**
 * The table's key + GSI shape, as data — provision it yourself in CDK / CloudFormation /
 * Terraform (the production path; `ensureTable` needs `CreateTable` IAM and sits outside your
 * IaC's drift/backup control). `pk`/`sk` are the single-table primary key; `gsi1` orders due
 * timers and claimable jobs. `PAY_PER_REQUEST` and any PITR/tags are your choice at provision
 * time — the engine only requires these keys and this one index.
 */
declare const tableSpec: (table?: string) => CreateTableCommandInput;
/**
 * The exact IAM actions the backend needs on the workflow table + its `gsi1`. `TransactWriteItems`
 * and `ConditionCheckItem` are the important extras — a CDK `grantReadWriteData` omits them, and
 * without them every atomic outbox write fails. Grant these on `arn:…:table/<name>` and
 * `arn:…:table/<name>/index/*`.
 */
declare const REQUIRED_IAM_ACTIONS: readonly string[];
/**
 * Create the table + its GSI if absent, then block until active. Idempotent (a
 * `ResourceInUseException` is swallowed). A dev / quickstart convenience — production should
 * provision from {@link tableSpec} in its own IaC instead.
 */
declare const ensureTable: (low: DynamoDBClient, table?: string) => Promise<void>;
//#endregion
//#region src/backend.d.ts
interface DynamoBackendOpts {
  /** The single table all four ports share. Default {@link DEFAULT_TABLE}. */
  table?: string;
  /** Id generator for runs, signals, and lease tokens. Defaults to {@link newId} (RFC-4122 v4). */
  id?: IdGen;
}
/**
 * The DynamoDB {@link Backend}: the four ports over one document client and one table. Store,
 * Queue, and Timer share that table, so an outbox commits as one `TransactWriteItems` — the
 * single transactional domain the seam requires. Pass a client from {@link docClient}, or any
 * object exposing `send`.
 */
declare const createDynamoBackend: (doc: Doc, opts?: DynamoBackendOpts) => Backend;
//#endregion
export { DEFAULT_TABLE, type Doc, type DynamoBackendOpts, REQUIRED_IAM_ACTIONS, createDynamoBackend, docClient, ensureTable, tableSpec };
```
